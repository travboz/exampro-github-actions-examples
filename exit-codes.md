# Using Exit Codes in Actions

We can use `exit codes` to set the status of an action.

Example bash scripts using exit codes:

- [Bash script](./exit-code-example-bash.sh)
- [Docker container script](./exit-code-example-docker.sh)

## Why set exit codes?

We can then raise our own errors that we want to have.
