name: Using Schedule event trigger with CRON

on:
  schedule:
    - cron: "*/30 * * * 6"

jobs:
  hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Echo current time
        run: |
          echo "The current server time is $(date)"
